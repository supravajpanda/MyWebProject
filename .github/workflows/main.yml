name: myFirstWorkflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
  steps:
      - name: first greeting
        run: echo "Hello"
      - name: Bye
        run: echo "Done"
      
